{
  "name": "restaurant-reservation-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "npm run typecheck && npm run test && npm run build",
    "verify:browser": "node scripts/verify-browser.mjs",
    "seed": "node scripts/seed-demo-data.mjs",
    "env:check": "node scripts/check-env.mjs",
    "agentphone:setup": "node scripts/setup-agentphone.mjs"
  },
  "dependencies": {
    "@google/genai": "latest",
    "lucide-react": "latest",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest",
    "vitest": "latest"
  },
  "overrides": {
    "postcss": "^8.5.14"
  }
}
